# 贡献指南

欢迎提交 Bug 报告、平台页面变化信息和可验证的改进。提交前请遵守以下要求。

## 提交 Bug

- 使用 Issue 模板，写明平台、发生阶段、预期行为与实际行为。
- 提供经脱敏的页面文本、截图或终端错误；不要提交 Cookie、Token、二维码、手机号、商品后台数据或原始视频。
- 说明是否为首次运行、是否为定时发布、是否含 AI、是否挂商品，以及是否已完成上传。
- 不要通过 Issue 要求绕过平台限制、登录、验证码或最终发布授权。

## 提交代码

1. 保持每次变更聚焦于一个平台行为、编排规则或文档主题。
2. 平台写入逻辑必须遵循 `inspect -> action -> verify`，并保持幂等。
3. 不得让修改绕过最终发布授权、声明真实性、任务空间所有权或发布成功证据。
4. 为可自动验证的行为添加测试，并运行：

```bash
node --test scripts/tests/*.test.mjs scripts/v2/tests/*.test.mjs
python3 /Users/a007/.codex/skills/.system/skill-creator/scripts/quick_validate.py .
```

5. 如果修改真实页面适配器，请说明使用哪个平台、哪个阶段和何种页面证据完成了人工验收；不要提交个人账号数据。

## 不接受的改动

- 账号、Cookie、Token、密码、个人配置或测试视频。
- 通过固定坐标、脆弱 class 哈希或注入伪造话题实体来代替页面证据。
- 无上限重试、重复上传活动文件，或对已发布平台再次操作。
